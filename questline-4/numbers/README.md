Palindrome
Convert the given int to string, so that I can use string slicing to reverse the given string. The reversed string is then tested to check if it is the same as the initial string. True or False is returned accordingly.

Roman to Integer
The given table of values is incomplete with regard to the conversion rules. Added the Roman equivalents of 900, 400, 90, 40, 9, 4 in their respective positions in the table. Added the table for reference to Python as a nested List starting with ["M", 1000]. Iterated through each integer value within the inner list and performed floor division on the input number. The quotient is the number of times the corresponding roman value is added to the string. Ensured that the inputted number is changed with each floor division by performing an additional modulus operation. The final string of roman values is returned as the roman number of the inputted number.
